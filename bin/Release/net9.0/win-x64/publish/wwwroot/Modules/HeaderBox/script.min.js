class HeaderBox{constructor(id=null){id!=null&&(this.Id=id.length>0?id:this.constructor.name);this.UrlContent=`/Modules/${this.constructor.name}/content`;this._LogBase=`Page component: ${this.constructor.name}. id: ${this.Id}`;this._IsDebug=document.URL.includes("localhost");this._IsDebug&&console.log(`${this._LogBase} Ready to use`);this.__Title=""}initial_title(title=""){this.__Title=title}push(target,position="afterend"){this._IsDebug&&console.log(`${this._LogBase}.push()`);target instanceof HTMLElement||(target=document.getElementById(target));target.insertAdjacentHTML(position,this._html())}_html(){this._IsDebug&&console.log(`${this._LogBase}._html()`);const idH=this.Id?`id="${this.Id}"`:"";return`
<header ${idH} class="${this.constructor.name}">
    <div>
        <a href="/">
            ${this.__Title}
        </a>
        <nav>
            <ul></ul>
        </nav>
    </div>
</header>`}push_menu_btn(model){let position="beforeend";model.position!=undefined&&model.position!=null&&(position=model.position);document.querySelector("#"+this.Id+" > div:first-child > nav > ul").insertAdjacentHTML(position,this._menu_li_html(model))}_menu_li_html(model){this._IsDebug&&console.log(this._LogBase+"._menu_li()");let tp=0;(tp==undefined||tp==null)&&(tp=0);let html="";if(model!=undefined&&model!=null){let idAttr="";model.id!=undefined&&model.id!=null&&model.id.length>0&&(idAttr='id="'+model.id+'"');let type="simple";model.type!=undefined&&model.type!=null&&model.type.length>0&&(type=model.type);let typeAttr='data-type="'+type+'"',hrefAttr="";model.url!=undefined&&model.url!=null&&model.url.length>0&&(hrefAttr='href="'+model.url+'"');let title="";model.title!=undefined&&model.title!=null&&model.title.length>0&&(title=model.title);let icon="";model.icon!=undefined&&model.icon!=null&&model.icon.length>0&&(icon=model.icon);let altAttr="";model.alt!=undefined&&model.alt!=null&&model.alt.length>0&&(altAttr='alt="'+model.alt+'"');let span="";title.length>0&&(span="<span>"+title+"<\/span>");let picture="";icon.length>0&&(picture='<picture>    <source srcset="'+icon+'-dark.png" media="(prefers-color-scheme: dark)">    <img src="'+icon+'-light.png" '+altAttr+"><\/picture>");let html_sub="";html_sub="<a "+hrefAttr+">    <div>    "+span+"    "+picture+"    <\/div><\/a>";html+="<li "+idAttr+" "+typeAttr+">"+html_sub+"<\/li>"}return html}}